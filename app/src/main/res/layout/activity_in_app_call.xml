<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent" android:layout_height="match_parent"
    android:orientation="vertical" android:background="@color/bg_page"
    android:gravity="center">

    <!-- Call in progress bar (Phase 2 only) -->
    <LinearLayout android:id="@+id/callProgressBar"
        android:layout_width="match_parent" android:layout_height="44dp"
        android:background="#0F4A3A" android:gravity="center"
        android:layout_gravity="top" android:visibility="gone">
        <TextView android:layout_width="wrap_content" android:layout_height="wrap_content"
            android:text="Call in progress" android:textSize="13sp"
            android:textColor="#1D9E75" android:textStyle="bold"/>
    </LinearLayout>

    <LinearLayout android:layout_width="match_parent" android:layout_height="0dp"
        android:layout_weight="1" android:orientation="vertical"
        android:gravity="center" android:padding="32dp">

        <!-- Pulse ring + Avatar -->
        <FrameLayout android:layout_width="100dp" android:layout_height="100dp"
            android:layout_gravity="center_horizontal" android:layout_marginBottom="20dp">
            <View android:id="@+id/callPulseRing"
                android:layout_width="100dp" android:layout_height="100dp"
                android:background="@drawable/bg_avatar_teal" android:alpha="0.3"/>
            <FrameLayout android:layout_width="80dp" android:layout_height="80dp"
                android:layout_gravity="center"
                android:background="@drawable/bg_avatar_teal">
                <TextView android:id="@+id/tvCallerInitial"
                    android:layout_width="wrap_content" android:layout_height="wrap_content"
                    android:layout_gravity="center"
                    android:text="R" android:textSize="32sp"
                    android:textStyle="bold" android:textColor="#1D9E75"/>
            </FrameLayout>
        </FrameLayout>

        <TextView android:id="@+id/tvCallerName"
            android:layout_width="wrap_content" android:layout_height="wrap_content"
            android:text="Ramesh Kumar" android:textSize="22sp"
            android:textStyle="bold" android:textColor="@color/text_primary"
            android:layout_marginBottom="6dp"/>

        <TextView android:id="@+id/tvCallStatus"
            android:layout_width="wrap_content" android:layout_height="wrap_content"
            android:text="Connecting..." android:textSize="14sp" android:textColor="@color/text_secondary"
            android:layout_marginBottom="4dp"/>

        <TextView android:id="@+id/tvCallTimer"
            android:layout_width="wrap_content" android:layout_height="wrap_content"
            android:text="00:00" android:textSize="28sp"
            android:textStyle="bold" android:textColor="@color/text_primary"
            android:visibility="gone" android:layout_marginBottom="8dp"/>

        <TextView android:layout_width="wrap_content" android:layout_height="wrap_content"
            android:text="🔒 Number masked for privacy"
            android:textSize="11sp" android:textColor="@color/text_muted"/>
    </LinearLayout>

    <!-- Controls -->
    <LinearLayout android:id="@+id/callControls"
        android:layout_width="match_parent" android:layout_height="wrap_content"
        android:orientation="horizontal" android:gravity="center"
        android:paddingBottom="48dp" android:paddingTop="16dp">

        <!-- Mute -->
        <LinearLayout android:id="@+id/btnMute"
            android:layout_width="56dp" android:layout_height="56dp"
            android:background="@drawable/bg_call_control_btn"
            android:gravity="center" android:alpha="0.4"
            android:layout_marginEnd="24dp"
            android:clickable="true" android:focusable="true">
            <ImageView android:layout_width="24dp" android:layout_height="24dp"
                android:src="@drawable/ic_mic"/>
        </LinearLayout>

        <!-- End Call -->
        <LinearLayout android:id="@+id/btnEndCall"
            android:layout_width="68dp" android:layout_height="68dp"
            android:background="@drawable/bg_call_end_btn"
            android:gravity="center" android:layout_marginEnd="24dp"
            android:clickable="true" android:focusable="true">
            <ImageView android:layout_width="28dp" android:layout_height="28dp"
                android:src="@drawable/ic_call_end"/>
        </LinearLayout>

        <!-- Speaker -->
        <LinearLayout android:id="@+id/btnSpeaker"
            android:layout_width="56dp" android:layout_height="56dp"
            android:background="@drawable/bg_call_control_btn"
            android:gravity="center" android:alpha="0.4"
            android:clickable="true" android:focusable="true">
            <ImageView android:layout_width="24dp" android:layout_height="24dp"
                android:src="@drawable/ic_volume_up"/>
        </LinearLayout>
    </LinearLayout>

    <!-- Call ended actions -->
    <LinearLayout android:id="@+id/callEndedActions"
        android:layout_width="match_parent" android:layout_height="wrap_content"
        android:orientation="horizontal" android:paddingStart="24dp"
        android:paddingEnd="24dp" android:paddingBottom="40dp"
        android:visibility="gone">
        <Button android:id="@+id/btnCallAgain"
            android:layout_width="0dp" android:layout_height="48dp" android:layout_weight="1"
            android:text="Call Again" android:textAllCaps="false"
            android:background="@drawable/bg_teal_outline_btn"
            android:textColor="#1D9E75" android:layout_marginEnd="10dp"/>
        <Button android:id="@+id/btnBackToDriver"
            android:layout_width="0dp" android:layout_height="48dp" android:layout_weight="1"
            android:text="Back to Driver"
            android:textAllCaps="false" android:backgroundTint="#1D9E75" android:textColor="#FFFFFF"/>
    </LinearLayout>
</LinearLayout>
